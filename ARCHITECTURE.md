# GoBeyond Travel - Architecture Diagrams & Visual Guide

## 📐 System Architecture Overview

```

┌─────────────────────────────────────────────────────────────────────┐
│                         FLUTTER APPLICATION                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  ┌────────────────────────────────────────────────────────────┐    │
│  │              PRESENTATION LAYER (UI)                       │    │
│  │                                                             │    │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │    │
│  │  │ Screens  │  │ Widgets  │  │ Providers│  │  States  │  │    │
│  │  │  (.dart) │  │  (.dart) │  │(Riverpod)│  │(Notifiers)│  │    │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘  │    │
│  │       │              │              │              │       │    │
│  └───────┼──────────────┼──────────────┼──────────────┼───────┘    │
│          │              │              │              │             │
│          └──────────────┴──────────────┴──────────────┘             │
│                             │                                        │
│  ┌──────────────────────────▼──────────────────────────────────┐   │
│  │                 DOMAIN LAYER (Business Logic)               │   │
│  │                                                              │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │   │
│  │  │ Entities │  │ Use Cases│  │Repository│  │ Business │   │   │
│  │  │  (Pure)  │  │  Logic   │  │Interfaces│  │  Rules   │   │   │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │   │
│  │       │              │              │              │        │   │
│  └───────┼──────────────┼──────────────┼──────────────┼────────┘   │
│          │              │              │              │             │
│          └──────────────┴──────────────┴──────────────┘             │
│                             │                                        │
│  ┌──────────────────────────▼──────────────────────────────────┐   │
│  │                   DATA LAYER (Data Access)                  │   │
│  │                                                              │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │   │
│  │  │  Models  │  │   DAOs   │  │Repository│  │   Sync   │   │   │
│  │  │(Mappers) │  │(SQLite)  │  │   Impl   │  │ Manager  │   │   │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │   │
│  │       │              │              │              │        │   │
│  └───────┼──────────────┼──────────────┼──────────────┼────────┘   │
│          │              │              │              │             │
│          └──────────────┴──────────────┴──────────────┘             │
│                             │                                        │
└─────────────────────────────┼────────────────────────────────────────┘
                              │
                 ┌────────────┴────────────┐
                 │                         │
        ┌────────▼────────┐      ┌────────▼────────┐
        │ SQLite Database │      │   Remote API    │
        │  (Offline-First)│      │ (Future Sync)   │
        └─────────────────┘      └─────────────────┘
```

---

## 🗄️ Database Schema Visualization

```
┌─────────────────────────────────────────────────────────────────┐
│                      USERS TABLE                                 │
├──────────────┬──────────────────────────────────────────────────┤
│ id (PK)      │ INTEGER PRIMARY KEY AUTOINCREMENT               │
│ name         │ TEXT NOT NULL                                    │
│ email        │ TEXT UNIQUE NOT NULL                             │
│ password     │ TEXT NOT NULL (hashed)                           │
│ phone        │ TEXT                                             │
│ profile_photo│ TEXT                                             │
│ role         │ TEXT (user/admin)                                │
│ created_at   │ INTEGER                                          │
│ sync_status  │ TEXT (synced/pending/conflict)                   │
└──────────────┴──────────────────────────────────────────────────┘
       │ 1
       │
       │ N
       ▼
┌─────────────────────────────────────────────────────────────────┐
│                    BOOKINGS TABLE                                │
├──────────────┬──────────────────────────────────────────────────┤
│ id (PK)      │ INTEGER PRIMARY KEY AUTOINCREMENT               │
│ user_id (FK) │ INTEGER → users.id                               │
│ listing_id   │ INTEGER → listings.id                            │
│ check_in     │ INTEGER (timestamp)                              │
│ check_out    │ INTEGER (timestamp)                              │
│ guests       │ INTEGER                                          │
│ total_price  │ REAL                                             │
│ status       │ TEXT (booked/cancelled/completed)                │
│ booking_ref  │ TEXT UNIQUE                                      │
│ created_at   │ INTEGER                                          │
│ sync_status  │ TEXT                                             │
└──────────────┴──────────────────────────────────────────────────┘
       │ N                                    ▲ 1
       │                                      │
       │ 1                                    │ N
       ▼                                      │
┌─────────────────────────┐    ┌─────────────────────────────────┐
│  ITINERARY_ITEMS TABLE  │    │      LISTINGS TABLE             │
├─────────────────────────┤    ├─────────────────────────────────┤
│ id (PK)                 │    │ id (PK)                         │
│ itinerary_id (FK)       │    │ title                           │
│ booking_id (FK)         │    │ type (hotel/flight/experience)  │
│ item_type               │    │ location                        │
│ title                   │    │ price                           │
│ scheduled_time          │    │ description                     │
│ sync_status             │    │ rating                          │
└─────────────────────────┘    │ photos (JSON)                   │
       ▲ N                      │ amenities (JSON)                │
       │                        │ sync_status                     │
       │ 1                      └─────────────────────────────────┘
       │                                     ▲ 1
┌─────────────────────────┐                 │
│   ITINERARIES TABLE     │                 │ N
├─────────────────────────┤                 │
│ id (PK)                 │    ┌────────────────────────┐
│ user_id (FK)            │    │   WISHLISTS TABLE      │
│ title                   │    ├────────────────────────┤
│ start_date              │    │ id (PK)                │
│ end_date                │    │ user_id (FK)           │
│ destination             │    │ listing_id (FK)        │
│ notes                   │    │ created_at             │
│ sync_status             │    │ sync_status            │
└─────────────────────────┘    └────────────────────────┘
       ▲ N                              ▲ N
       │                                │
       │ 1                              │ 1
       └────────────────────────────────┘
              USERS (1:N relationships)

┌─────────────────────────────────────────────────────────────────┐
│                     FEEDBACKS TABLE                              │
├──────────────┬──────────────────────────────────────────────────┤
│ id (PK)      │ INTEGER PRIMARY KEY                              │
│ user_id (FK) │ INTEGER → users.id                               │
│ listing_id   │ INTEGER → listings.id                            │
│ booking_id   │ INTEGER → bookings.id (nullable)                 │
│ rating       │ INTEGER (1-5)                                    │
│ comment      │ TEXT                                             │
│ created_at   │ INTEGER                                          │
│ sync_status  │ TEXT                                             │
└──────────────┴──────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                      REWARDS TABLE                               │
├──────────────┬──────────────────────────────────────────────────┤
│ id (PK)      │ INTEGER PRIMARY KEY                              │
│ user_id (FK) │ INTEGER → users.id                               │
│ points       │ INTEGER                                          │
│ promo_code   │ TEXT UNIQUE                                      │
│ discount_%   │ REAL                                             │
│ expiry_date  │ INTEGER                                          │
│ is_redeemed  │ INTEGER (0/1)                                    │
│ sync_status  │ TEXT                                             │
└──────────────┴──────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Architecture

### Read Operation Flow

```
┌──────────────┐
│   UI Screen  │
│  (Widget)    │
└──────┬───────┘
       │ 1. Watch Provider
       ▼
┌──────────────────┐
│ Riverpod Provider│
│ (FutureProvider) │
└──────┬───────────┘
       │ 2. Call Use Case / Repository
       ▼
┌──────────────────┐
│   Repository     │
│ (Domain Layer)   │
└──────┬───────────┘
       │ 3. Request Data
       ▼
┌──────────────────┐
│  Data Source     │
│    (DAO)         │
└──────┬───────────┘
       │ 4. Execute Query
       ▼
┌──────────────────┐
│ SQLite Database  │
└──────┬───────────┘
       │ 5. Return Map
       ▼
┌──────────────────┐
│  Data Source     │
│ Map → Model      │
└──────┬───────────┘
       │ 6. Return Model
       ▼
┌──────────────────┐
│   Repository     │
│ Model → Entity   │
└──────┬───────────┘
       │ 7. Return Entity (or Failure)
       ▼
┌──────────────────┐
│    Provider      │
│  Update State    │
└──────┬───────────┘
       │ 8. Notify Listeners
       ▼
┌──────────────────┐
│   UI Screen      │
│    Rebuilds      │
└──────────────────┘
```

### Write Operation Flow

```
┌──────────────┐
│   UI Screen  │
│ User Action  │
└──────┬───────┘
       │ 1. Call Notifier Method
       ▼
┌──────────────────┐
│ StateNotifier    │
│ (BookingNotifier)│
└──────┬───────────┘
       │ 2. Set Loading State
       │ 3. Call Repository
       ▼
┌──────────────────┐
│   Repository     │
│ Entity → Model   │
└──────┬───────────┘
       │ 4. Convert & Save
       ▼
┌──────────────────┐
│  Data Source     │
│ Model → Map      │
└──────┬───────────┘
       │ 5. Insert/Update
       ▼
┌──────────────────┐
│ SQLite Database  │
│ sync_status:     │
│ 'pending'        │
└──────┬───────────┘
       │ 6. Return ID
       ▼
┌──────────────────┐
│  Data Source     │
│ Map → Model      │
└──────┬───────────┘
       │ 7. Return Model
       ▼
┌──────────────────┐
│   Repository     │
│ Model → Entity   │
└──────┬───────────┘
       │ 8. Return Success
       ▼
┌──────────────────┐
│ StateNotifier    │
│ Update State     │
└──────┬───────────┘
       │ 9. Notify UI
       ▼
┌──────────────────┐      ┌──────────────────┐
│   UI Screen      │      │  Sync Manager    │
│   Refreshes      │      │ (Background)     │
└──────────────────┘      │ Syncs to API     │
                          └──────────────────┘
```

---

## 🎯 Feature Module Structure

```
features/booking/
│
├── data/                           # Data Layer
│   ├── models/
│   │   └── booking_model.dart      # SQLite/API serialization
│   ├── datasources/
│   │   ├── booking_local_datasource.dart   # SQLite operations
│   │   └── booking_remote_datasource.dart  # API calls (future)
│   └── repositories/
│       └── booking_repository_impl.dart    # Implements domain interface
│
├── domain/                         # Business Logic Layer
│   ├── entities/
│   │   └── booking.dart            # Pure Dart class
│   ├── repositories/
│   │   └── booking_repository.dart # Abstract interface
│   └── usecases/
│       ├── create_booking.dart     # Single responsibility
│       ├── get_user_bookings.dart
│       └── cancel_booking.dart
│
└── presentation/                   # UI Layer
    ├── providers/
    │   └── booking_provider.dart   # Riverpod providers
    ├── screens/
    │   ├── booking_screen.dart
    │   ├── booking_history_screen.dart
    │   └── booking_detail_screen.dart
    └── widgets/
        ├── booking_card.dart
        └── booking_filter.dart
```

---

## 🔐 Dependency Injection Flow

```
┌─────────────────────────────────────────────────────────┐
│                   ProviderScope (Root)                   │
└────────────────────────┬────────────────────────────────┘
                         │
         ┌───────────────┼───────────────┐
         │               │               │
         ▼               ▼               ▼
┌────────────────┐ ┌────────────┐ ┌────────────┐
│ DatabaseHelper │ │Connectivity│ │   Theme    │
│   Provider     │ │  Provider  │ │  Provider  │
└────────┬───────┘ └──────┬─────┘ └────────────┘
         │                │
         └────────┬───────┘
                  │
         ┌────────▼────────┐
         │  Data Sources   │
         │    Providers    │
         └────────┬────────┘
                  │
         ┌────────▼────────┐
         │  Repositories   │
         │    Providers    │
         └────────┬────────┘
                  │
         ┌────────▼────────┐
         │  State/Logic    │
         │    Providers    │
         └────────┬────────┘
                  │
         ┌────────▼────────┐
         │   UI Widgets    │
         │  (Consumers)    │
         └─────────────────┘
```

---

## 📱 Navigation Flow

```
                    ┌──────────────┐
                    │ Splash Screen│
                    └──────┬───────┘
                           │
                           ▼
                    ┌──────────────┐
                 ┌─►│ Login Screen │
                 │  └──────┬───────┘
                 │         │ Auth Success
                 │         ▼
                 │  ┌────────────────────────────┐
                 │  │    Main Screen (Bottom Nav)│
                 │  └────────────────────────────┘
                 │         │
                 │    ┌────┼────┬────┬────┐
                 │    │    │    │    │    │
   Auth Failure  │    ▼    ▼    ▼    ▼    ▼
   ──────────────┘  Exp  Wish Book Itin Prof
                    lore  list ings rary ile
                      │    │    │    │    │
                      │    └────┼────┼────┼──► Detail Screens
                      │         │    │    │
                      └─────────┼────┘    └──► Settings/Rewards
                                │
                                └──► Booking Flow
                                       │
                                       └──► Confirmation
```

---

## 🔄 Offline-First Sync Strategy

```
┌─────────────────────────────────────────────────────────┐
│                    User Creates Booking                  │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│      Save to SQLite (sync_status = 'pending')           │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│           UI Shows Success Immediately                   │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│         Background: Check Internet Connection            │
└────────┬────────────────────────────────────────┬───────┘
         │ Connected                              │ Offline
         ▼                                        ▼
┌─────────────────────┐               ┌──────────────────┐
│  Send to API Server │               │  Queue for Later │
└────────┬────────────┘               │  Retry on Network│
         │                             └──────────────────┘
    ┌────┴────┐
    │ Success │ Failure
    ▼         ▼
┌────────┐ ┌──────────┐
│ Mark as│ │ Mark as  │
│ Synced │ │ Conflict │
└────────┘ └──────────┘
               │
               ▼
        ┌──────────────┐
        │ Show Conflict│
        │  Resolution  │
        │    Dialog    │
        └──────────────┘
```

---

## 🧪 Testing Architecture

```
┌──────────────────────────────────────────────────────┐
│                   UNIT TESTS                          │
│  • Entity logic                                      │
│  • Use case business logic                           │
│  • Repository implementations                        │
│  • Data source operations                            │
└──────────────────────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────┐
│                 WIDGET TESTS                          │
│  • Individual widgets                                │
│  • Screen layouts                                    │
│  • User interactions                                 │
│  • Provider state changes                            │
└──────────────────────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────┐
│              INTEGRATION TESTS                        │
│  • Complete user flows                               │
│  • Database operations                               │
│  • Navigation flows                                  │
│  • End-to-end scenarios                              │
└──────────────────────────────────────────────────────┘
```

---

## 🚀 Performance Optimization Strategy

```
┌─────────────────────────────────────────────────┐
│          DATABASE OPTIMIZATIONS                  │
│  • Indexes on frequently queried columns        │
│  • Batch operations for multiple inserts        │
│  • Connection pooling (singleton DatabaseHelper)│
│  • Prepared statements (parameterized queries)  │
└─────────────────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────┐
│            UI OPTIMIZATIONS                      │
│  • Lazy loading with pagination                 │
│  • Image caching (cached_network_image)         │
│  • Debounced search inputs                      │
│  • const constructors for widgets               │
└─────────────────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────┐
│         STATE MANAGEMENT                         │
│  • Granular provider rebuilds                   │
│  • family/autoDispose for scoped state          │
│  • Selective widget listening                   │
└─────────────────────────────────────────────────┘
```

---

This architecture ensures:
✅ Clean separation of concerns
✅ Offline-first functionality
✅ Easy testing at all levels
✅ Scalability for future features
✅ Type safety with Riverpod
✅ Performance optimization
✅ Clear data flow
